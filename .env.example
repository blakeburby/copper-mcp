# Copy to .env for local testing (the server also reads these straight from the
# Claude Desktop config `env` block — see the README). Never commit real values.
COPPER_API_KEY=your_copper_api_key_here
COPPER_USER_EMAIL=you@yourcompany.com
